server:
  port: 9093


eureka:
  instance:
    appname: documentation-application
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:3025/v1.

swagger:
  config:
    refreshrate: 5000